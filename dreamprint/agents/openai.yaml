interface:
  display_name: "Dream Print"
  short_description: "Interview-first service architecture on paper — E0/P1/R1/P2/P3 Mermaid pages."
  default_prompt: "Use Dream Print. Run the readiness gate and interview first, then produce the architecture package (E0, P1, R1, P2, P3)."
